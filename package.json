{
  "name": "letui",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build-ffi": "cd letui-ffi && cargo build --release",
    "dev": "bun run build-ffi && bun run components.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
