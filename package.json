{
  "name": "letui",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build-ffi": "cd letui-ffi && cargo build --release",
    "dev": "bun run build-ffi && bun run examples.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
